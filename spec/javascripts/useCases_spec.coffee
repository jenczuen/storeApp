describe "UseCase", ->
	it "initializes database", ->
		console.log("fal")
		useCase.init()
#		expect(useCase.initialized).toEqual(true)
#		expect(useCase.allProducts[0].name).toEqual("Al Hodkiewicz")

	it "shows home page", ->
		expect(true).toEqual(true)
#		useCase.showHomePage()

	it "shows all categories", ->
		expect(true).toEqual(true)	
#		useCase.showAllCategories()

	it "shows single category", ->
		expect(true).toEqual(true)	
#		useCase.showCategory()

	it "shows single product", ->
		expect(true).toEqual(true)	
#		useCase.showProduct()

	it "shows buyers cart", ->
		expect(true).toEqual(true)	
#		useCase.showCart()

	it "updates the small cart", ->
		expect(true).toEqual(true)	
#		useCase.updateSmallCart()

	it "add products to the cart", ->
		expect(true).toEqual(true)	
#		useCase.addProductToCart()

	it "removes product from the cart", ->
		expect(true).toEqual(true)	
#		useCase.removeProductFromCart()

	it "shows form for buyers personal data if buyer is new", ->
		expect(true).toEqual(true)	
#		useCase.showFormForBuyerPersonalData()

	it "saves buyers personal data", ->
		expect(true).toEqual(true)	
#		useCase.saveBuyerPersonalData()

	it "confirms an order", ->
		expect(true).toEqual(true)	
#		useCase.orderConfirmed()

	it "initializes database", ->
		expect(true).toEqual(true)	
#		useCase.search()