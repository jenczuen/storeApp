<h1><%= @category.name %></h1>

<center>
<% @products.each do |product| %>
	<li>
		<%= product.author %> <%= link_to product.title, product %>
	</li>
<% end %>

<%= link_to "Back to categories", "/categories"%>
</center>