<%= @item+@product.title %> <br>
<%= @text %>
<%= form_for [:admin, @product] do |f| %>
	<%= f.hidden_field :change, :value => @key %>	
	<%= f.hidden_field :id, :value => @product.id %>		
	<%= f.text_field @key %>
	<%= f.submit "Zmien"%>	
<%end%>   			
